# 更新日志 (CHANGELOG)

## [v3.1] - 2026-06-18

### 后端稳定性修复

- 图片下载、模型正式调用、模型连接测试现在统一复用全局超时与代理配置，避免“答题可用但测试失败”或“图片题单独超时”的不一致。
- 修复 `httpx 0.28+` 下旧 `proxies` 参数不兼容的问题，统一改为兼容当前运行库的客户端构造方式。
- 模型调用重试现在复用同一个 HTTP 客户端，并复用已构造的图文请求体，避免图片题在重试时反复下载同一批图片。
- 思考模式状态改为按“是否真正下发 reasoning 参数”计算，不再把未实际启用思考的请求错误标记为“深度思考”。
- 全局 `ENABLE_REASONING` 现在会真正参与请求构造，不再只是影响标签显示。
- 配置保存新增数值和布尔校验，避免把非法 `.env` 写回磁盘后导致重启失败。
- 删除模型和修改题型映射时新增保存失败回滚，避免内存态与 `custom_models.json` 分叉。
- 填空题的请求追踪日志改为在清理编辑器残留后写入，排障时看到的 `normalized_options` 与实际入模数据一致。
- 健康检查与响应头版本号统一更新为 `3.1.0`，CSV 中对未知提供商不再错误套用 DeepSeek 价格估算。

### 新增与升级

- 模型来源改为完全基于模型管理器，答题运行时不再依赖 `.env` 中的 `MODEL_PROVIDER / PREFER_MODEL / IMAGE_MODEL / DEEPSEEK_* / DOUBAO_*` 做实际选模。
- 旧的系统模型迁移为 3 个一次性初始化的内置预设：
  - `preset_deepseek_v4_flash`
  - `preset_deepseek_v4_pro`
  - `preset_doubao`
- 内置预设与普通模型一致，支持编辑、删除、测试；删除后重启不会自动恢复。
- 新增 OpenAI 最新协议适配，模型可按需选择：
  - `openai_compat`
  - `responses`
  - `chat_completions`
- OpenAI Responses API 已适配高强度思考参数，并在请求中显式启用 `reasoning.summary` 返回思考摘要。

### 多模态与答题修复

- 图片题请求改为图文混排，按题干与选项中的原始出现顺序把图片插回消息体。
- 选项图片会附加 `Option A/B/C...` 标记，保证模型知道图片与选项字母的对应关系。
- OpenAI 兼容多模态模型现在会真正发送图片，而不是只把图片 URL 当作普通文本。
- 图片下载失败或无法完整构造混排时，会自动回退到传统模式并记录清晰日志。
- 修复填空题误把富文本编辑器脚本残留当作“选项”的问题，后端会自动忽略这类脏数据。
- 修复图片选项题在返回字母答案后又被后处理替换成图片 URL 的问题。

### 模型映射与配置体验

- 图片题映射现在强制只允许多模态模型：
  - 前端加载与保存时会自动清理非多模态候选
  - 后端加载、保存和模型更新时也会同步清理
- `/api/config` 与 `/api/health` 的运行时摘要改为基于模型管理器返回：
  - 已配置模型数
  - 已启用模型数
  - 已映射题型
  - 是否存在可用多模态模型
  - 当前整体是否可答题
- 配置页移除了旧的模型配置入口，模型管理成为唯一模型配置入口。
- 配置页中的“思考模式”入口做了层级收敛，模型能力与题型开关放在模型管理页，配置页只保留全局兼容策略与通用 AI 参数。

### 前端优化

- 前端统一改为 Element Plus 原生图标组件，不再使用 emoji 作为页面标题、页签、按钮和提示符号。
- 模型编辑时会先请求 `/api/models/:id` 获取完整详情，避免把掩码密钥保存回去。
- 打包输出路径重新统一为 `dist/`，并同步更新了构建脚本与静态资源服务入口。

### 兼容性与运维

- `start.bat` 改为默认启动 `ocs_ai_answerer_advanced.py`。
- 保留 `.env` 中的通用运行参数用途，例如超时、重试、日志、代理、全局兼容思考策略等。
- 历史 `.env` 中的模型字段仅用于首次迁移，不再作为答题运行时来源。

## [v2.2] - 2025-11-11

### ✨ 新增功能

#### 🎨 Web配置管理面板
- **可视化配置界面**：通过浏览器图形界面管理所有配置项，无需手动编辑 `.env` 文件
- **实时状态监控**：在首页查看服务状态、当前模型、思考模式等实时信息
- **快速导航中心**：一键跳转到答题记录、API文档等功能页面
- **配置预览和导出**：实时预览配置内容、导出 `.env` 文件、重置为默认值

#### 🌐 新增路由
- `GET /` - 配置管理面板（Web UI）
- `GET /config` - 配置管理面板（别名）
- `GET /api/docs` - API接口文档页面
- `POST /api/config` - 保存配置到 `.env` 文件

#### 📚 API文档页面
- 完整的REST API接口文档
- 请求/响应示例
- 参数说明和错误码

### 🔄 改进

#### API 增强
- `GET /api/config` 现在返回所有配置项（包括运行时信息）
- `GET /?t=timestamp` 支持延迟测试（保持与旧版本兼容）

#### 用户体验
- 启动时显示配置面板访问提示
- Banner 增加配置面板和答题记录的链接
- 版本号更新为 v2.2

### 📝 文档

- 新增 `CONFIG_PANEL_GUIDE.md` - 配置面板使用指南
- 新增 `test_config_api.py` - 配置API测试脚本
- 新增 `config_panel.html` - 配置管理界面
- 新增 `api_docs.html` - API文档页面
- 更新 `README.md` - 添加Web界面说明

### 🔧 技术细节

- 配置面板采用纯HTML + CSS + JavaScript，无需额外依赖
- 配置数据以JSON格式通过REST API传输
- 配置保存采用标准 `.env` 格式，向下兼容
- 支持响应式设计，适配移动端浏览器

---

## [v2.1] - 2025-11-10

### ✨ 新增功能

#### 🖼️ 多模态支持增强
- 图片题目自动启用深度思考模式
- 优化图片下载和处理流程
- 支持图片URL过滤（自动过滤图标）

#### 🧠 智能模型选择
- `MODEL_PROVIDER=auto` 模式支持智能路由
- 纯文本题目使用 DeepSeek（成本低）
- 图片题目使用豆包（支持多模态）

### 🔄 改进

#### 性能优化
- 减少不必要的API调用
- 优化图片下载超时处理
- 增加重试机制

#### 日志优化
- CSV日志增加图片URL记录
- 日志文件线程安全写入
- 增加详细的调试信息

---

## [v2.0] - 2025-11-08

### ✨ 新增功能

#### 🤖 多模型支持
- 支持 DeepSeek 和豆包(Doubao)
- `MODEL_PROVIDER` 配置项：可选 `deepseek`、`doubao` 或 `auto`
- 智能模型选择：根据题目自动选择最佳模型

#### 💡 思考模式
- DeepSeek Reasoner 深度推理
- 豆包深度思考（reasoning_effort: low/medium/high）
- 多选题自动启用思考模式
- 图片题自动启用思考模式

#### 📊 答题记录可视化
- CSV日志记录所有答题信息
- 可视化统计页面（`/viewer`）
- 图表展示：题型分布、模型使用、准确率等

### 🔄 改进

#### API增强
- `/api/health` - 健康检查
- `/api/config` - 配置查询
- `/api/csv` - CSV数据获取
- `/viewer` - 可视化页面

#### 配置优化
- 支持 `.env` 文件配置
- 详细的配置说明（`env.template`）
- Token限制自动适配不同模型

---

## [v1.0] - 2025-11-05

### ✨ 初始版本

#### 基础功能
- 单模型支持（DeepSeek）
- 四种题型：单选、多选、判断、填空
- REST API接口
- OCS脚本集成

#### 核心特性
- 自然语言理解
- 答案清洗和匹配
- 错误处理和重试
- 日志记录

---

## 版本规划

### [v2.3] - 计划中
- [ ] 用户认证和权限管理
- [ ] 配置热重载（无需重启）
- [ ] 更多LLM模型支持
- [ ] 答题历史搜索和过滤
- [ ] 性能监控和告警

### [v3.0] - 未来展望
- [ ] 分布式部署支持
- [ ] 缓存机制优化
- [ ] 批量答题API
- [ ] WebSocket实时通信
- [ ] 插件系统

---

## 贡献指南

欢迎提交Issue和Pull Request！

- 报告Bug：https://github.com/lkd6666/ocs-ai-answerer/issues
- 功能建议：https://github.com/lkd6666/ocs-ai-answerer/discussions
- 代码贡献：Fork + PR

---

**维护者**: lkd6666  
**许可证**: MIT  
**更新频率**: 活跃开发中
